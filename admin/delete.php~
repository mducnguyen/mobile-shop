<?php
if(!isset($_GET['type']) || !isset($_GET['id']) || $_GET['type'] == NULL || $_GET['id'] == NULL){
  returnPage_admin();
}
  switch($_GET['type']){
    case 'product':
      $table = "Product";
      break;
    case 'admin':
      $table = 'Admin";
      break;
    case 'order':
  }
?>
